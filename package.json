{
  "name": "docs",
  "version": "1.1.2",
  "description": "proyecto de documentacion relevante acerca de tecnologias informaticas",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0",
    "preversion": "npm run changelog && npm run format && git add CHANGELOG.md",
    "prettier": "prettier --check .",
    "format": "prettier --write ."
  },
  "keywords": [
    "docs"
  ],
  "author": "jasvdev",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^3.0.3"
  }
}
